import { rgba } from '@revert/color'
import type { TColor } from '@revert/color'

export const LINE_HEIGHT = 20
export const COLOR_COMPONENT_NAME: TColor = rgba(0, 0, 255, 1)
export const COLOR_BLACK: TColor = rgba(0, 0, 0, 1)
