import { TColor } from '@revert/color'

export type TBorder = {
  color: TColor,
  radius?: number,
  width?: number,
  leftWidth?: number,
  topWidth?: number,
  rightWidth?: number,
  bottomWidth?: number,
  overflow?: number,
}
