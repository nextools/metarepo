export type TTransformProps = {
  x?: number,
  y?: number,
  rotate?: number,
  scale?: number,
  hOrigin?: 'left' | 'center' | 'right',
  vOrigin?: 'top' | 'center' | 'bottom',
  shouldUse3d?: boolean,
  shouldStretch?: boolean,
}
