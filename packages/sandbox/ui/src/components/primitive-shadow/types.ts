import { TColor } from '../../colors'

export type TPrimitiveShadow = {
  color: TColor,
  radius?: number,
  blurRadius?: number,
  spreadRadius?: number,
  offsetX?: number,
  offsetY?: number,
  overflow?: number,
}
