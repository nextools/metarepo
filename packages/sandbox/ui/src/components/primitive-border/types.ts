import { TColor } from '../../colors'

export type TPrimitiveBorder = {
  color: TColor,
  radius?: number,
  width?: number,
  leftWidth?: number,
  topWidth?: number,
  rightWidth?: number,
  bottomWidth?: number,
  overflow?: number,
}
